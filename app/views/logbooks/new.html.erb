<% content_for :title, "Paket öffnen" %>

<div class="mx-auto max-w-4xl space-y-6 px-4 sm:px-0">
  <section class="mt-8 rounded-[var(--radius-sm)] border border-[color:var(--glass-border)] bg-[color:var(--glass-bg)] px-6 py-6 shadow-sm backdrop-blur-xl">
    <div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
      <div>
        <h1 class="mb-2 text-2xl font-bold text-[color:var(--text)]">Paket öffnen</h1>
        <p class="mt-1 text-sm text-[color:var(--text-muted)]">
          Lege den Start einer neuen Packung fest, damit die Tassen sauber zugeordnet sind.
        </p>
      </div>
    </div>
  </section>

  <section class="rounded-[var(--radius-sm)] border border-[color:var(--glass-border)] bg-[color:var(--glass-bg)] shadow-sm backdrop-blur-xl">
    <div class="space-y-6 px-4 py-5 sm:p-6">
      <%= render "form", logbook: @logbook %>
    </div>
  </section>
</div>
