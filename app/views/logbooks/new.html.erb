<% content_for :title, "Add a Coffee" %>

<div class="md:grid md:grid-cols-3 md:gap-6">
  <div class="md:col-span-1">
      <div class="mb-10 mt-8">
      <div class="rounded-[var(--radius-sm)] border border-[color:var(--glass-border)] bg-[color:var(--glass-bg)] px-6 py-6 shadow-sm backdrop-blur-xl">
      <div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
        <div>
        <h1 class="mb-2 text-2xl font-bold text-[color:var(--text)]">Count your Coffee</h1>
        <p class="mt-1 text-sm text-[color:var(--text-muted)]">
          Add the amount of Coffee
        </p>
        </div>
      </div>
      </div>
      </div>
    <div class="px-4 sm:px-0">


      <div class="mt-5 md:mt-0 md:col-span-2">
        <div class="shadow-sm sm:rounded-[var(--radius-sm)] sm:overflow-hidden border border-[color:var(--glass-border)] bg-[color:var(--glass-bg)] backdrop-blur-xl">
          <div class="space-y-6 px-4 py-5 sm:p-6">
            <%= render "form", logbook: @logbook %>
          </div>
        </div>
      </div>
      <div class="mt-6">
        <div class="flex justify-end">
            <!-- %= link_to "CancBack to Counter", logbooks_path, class: "bg-white py-2 px-4 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-purple-500" % -->
         </div>
      </div>
    </div>
  </div>
</div>
